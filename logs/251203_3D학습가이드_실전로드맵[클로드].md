# 3D 웹 기술 학습 가이드 - 실전 로드맵

**작성일**: 2025-12-03
**대상**: 엘리콘 홈페이지 3D 구현을 위한 실전 학습
**목표**: 6주 내 Wing 로고 3D + 제품 뷰어 구현 가능한 수준 달성
**예상 투자 시간**: 주 10시간 × 6주 = 60시간

---

## 📋 목차

1. [학습 목표 및 마일스톤](#학습-목표-및-마일스톤)
2. [필수 준비사항](#필수-준비사항)
3. [6주 학습 로드맵](#6주-학습-로드맵)
4. [무료 vs 유료 리소스 가이드](#무료-vs-유료-리소스-가이드)
5. [실습 프로젝트 체크리스트](#실습-프로젝트-체크리스트)
6. [트러블슈팅 가이드](#트러블슈팅-가이드)

---

## 🎯 학습 목표 및 마일스톤

### 최종 목표
**엘리콘 홈페이지에 3D 요소 구현하여 제조사(한국OGK, 써모랩)에게 기술력 시연**

### 마일스톤

| Week | 마일스톤 | 성과물 | 제조사 임팩트 |
|------|---------|--------|-------------|
| **Week 1-2** | Three.js 기초 마스터 | 회전하는 큐브 + 조명 | ⭐ 기술 이해 |
| **Week 3** | React Three Fiber 통합 | Next.js 프로젝트에 3D 삽입 | ⭐⭐ 실전 준비 |
| **Week 4** | Blender 3D 모델링 | Wing 로고 3D 모델 제작 | ⭐⭐⭐ 브랜드 적용 |
| **Week 5** | 인터랙션 구현 | 마우스로 회전 가능한 로고 | ⭐⭐⭐⭐ 데모 가능 |
| **Week 6** | 최적화 + 배포 | 제조사 데모용 완성본 | ⭐⭐⭐⭐⭐ 제조사 시연 |

---

## 🛠 필수 준비사항

### 1. 개발 환경 설치

```bash
# Node.js 18+ 설치 확인
node -v  # v18.0.0 이상

# 프로젝트 생성 (이미 있다면 스킵)
cd /Users/elicon_mark/Developer/01_2_elicon_homepage

# Three.js 관련 패키지 설치
npm install three @react-three/fiber @react-three/drei @react-three/postprocessing
npm install -D @types/three

# 개발 서버 실행 확인
npm run dev
```

### 2. 3D 모델링 도구 설치

```bash
# Blender (무료 3D 모델링 소프트웨어)
# macOS: https://www.blender.org/download/
brew install --cask blender

# Spline (노코드 3D 툴, 선택사항)
# https://spline.design/download (macOS app)
```

### 3. VS Code 확장 프로그램

- **glTF Tools**: 3D 모델 미리보기
- **Three.js Snippets**: 코드 자동완성
- **Shader languages support**: 쉐이더 하이라이팅

### 4. 학습 계정 생성

- [ ] **Three.js Journey** (유료 $95, 강력 추천): https://threejs-journey.com
- [ ] **CodeSandbox** (무료, 실습용): https://codesandbox.io
- [ ] **Sketchfab** (무료, 3D 모델 다운로드): https://sketchfab.com

---

## 📅 6주 학습 로드맵

### **Week 1: Three.js 기초 완전 정복** (10시간)

#### Day 1-2: Three.js 핵심 개념 (4시간)

**목표**: Scene, Camera, Renderer 이해하기

**무료 튜토리얼**:
- [Three.js Fundamentals (한글)](https://threejs.org/manual/#ko/fundamentals) - 3시간
- 실습: 회전하는 큐브 만들기

```javascript
// app/test-3d/page.tsx
'use client';
import { useEffect, useRef } from 'react';
import * as THREE from 'three';

export default function Test3D() {
  const mountRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (!mountRef.current) return;

    // 1. Scene 생성
    const scene = new THREE.Scene();

    // 2. Camera 생성
    const camera = new THREE.PerspectiveCamera(
      75,
      window.innerWidth / window.innerHeight,
      0.1,
      1000
    );
    camera.position.z = 5;

    // 3. Renderer 생성
    const renderer = new THREE.WebGLRenderer({ antialias: true });
    renderer.setSize(window.innerWidth, window.innerHeight);
    mountRef.current.appendChild(renderer.domElement);

    // 4. Geometry + Material = Mesh
    const geometry = new THREE.BoxGeometry();
    const material = new THREE.MeshStandardMaterial({ color: 0x0066ff });
    const cube = new THREE.Mesh(geometry, material);
    scene.add(cube);

    // 5. 조명 추가
    const light = new THREE.DirectionalLight(0xffffff, 1);
    light.position.set(5, 5, 5);
    scene.add(light);

    const ambientLight = new THREE.AmbientLight(0xffffff, 0.5);
    scene.add(ambientLight);

    // 6. Animation Loop
    function animate() {
      requestAnimationFrame(animate);
      cube.rotation.x += 0.01;
      cube.rotation.y += 0.01;
      renderer.render(scene, camera);
    }
    animate();

    // Cleanup
    return () => {
      mountRef.current?.removeChild(renderer.domElement);
    };
  }, []);

  return <div ref={mountRef} />;
}
```

**체크리스트**:
- [ ] Scene, Camera, Renderer의 역할 설명 가능
- [ ] BoxGeometry로 큐브 생성
- [ ] Material의 color 변경
- [ ] DirectionalLight vs AmbientLight 차이 이해

#### Day 3-4: 조명과 재질 (3시간)

**목표**: 현실감 있는 조명 설정

**튜토리얼**:
- [Three.js Lighting (공식 문서)](https://threejs.org/manual/#ko/lights)
- 실습: 3가지 조명 조합

```javascript
// 조명 시스템 예시
const lights = {
  // 1. 환경광 (전체 밝기)
  ambient: new THREE.AmbientLight(0xffffff, 0.3),

  // 2. 주 조명 (그림자 포함)
  directional: new THREE.DirectionalLight(0xffffff, 0.8),

  // 3. 포인트 조명 (특정 지점)
  point: new THREE.PointLight(0xff0000, 1, 100)
};

lights.directional.position.set(10, 10, 5);
lights.directional.castShadow = true; // 그림자 활성화
```

**체크리스트**:
- [ ] AmbientLight, DirectionalLight, PointLight 차이 이해
- [ ] Shadow 설정 방법 숙지
- [ ] Intensity와 Color 조정

#### Day 5-7: 3D 모델 로드하기 (3시간)

**목표**: glTF 파일 불러오기

**튜토리얼**:
- [glTF Loader (Three.js 공식)](https://threejs.org/docs/#examples/en/loaders/GLTFLoader)

```javascript
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader';

const loader = new GLTFLoader();
loader.load(
  '/models/wing-logo.glb', // 모델 경로
  (gltf) => {
    scene.add(gltf.scene); // 성공 시
  },
  (xhr) => {
    console.log((xhr.loaded / xhr.total) * 100 + '% loaded'); // 진행률
  },
  (error) => {
    console.error('Error loading model:', error); // 에러 처리
  }
);
```

**무료 3D 모델 다운로드**:
- [Sketchfab Free Models](https://sketchfab.com/features/free-3d-models)
- 검색어: "logo", "product", "industrial"

**체크리스트**:
- [ ] GLTFLoader 사용법 숙지
- [ ] 로딩 진행률 표시 구현
- [ ] 에러 핸들링 추가

---

### **Week 2: React Three Fiber 통합** (10시간)

#### Day 1-3: R3F 기초 (5시간)

**목표**: React 컴포넌트로 3D 만들기

**유료 강의** (강력 추천):
- [Three.js Journey - React Three Fiber](https://threejs-journey.com) - Lesson 40-47
- 가격: $95 (평생 액세스, 업데이트 무료)
- ROI: 투자 대비 학습 속도 3배 향상

**무료 대안**:
- [React Three Fiber 공식 문서](https://docs.pmnd.rs/react-three-fiber/getting-started/introduction)

```typescript
// components/3d/RotatingCube.tsx
'use client';
import { Canvas } from '@react-three/fiber';
import { OrbitControls } from '@react-three/drei';

function Cube() {
  return (
    <mesh>
      <boxGeometry args={[1, 1, 1]} />
      <meshStandardMaterial color="hotpink" />
    </mesh>
  );
}

export function RotatingCube() {
  return (
    <Canvas>
      <ambientLight intensity={0.5} />
      <directionalLight position={[10, 10, 5]} />
      <Cube />
      <OrbitControls enableZoom={false} />
    </Canvas>
  );
}
```

**체크리스트**:
- [ ] Canvas 컴포넌트 이해
- [ ] mesh, geometry, material을 JSX로 작성
- [ ] OrbitControls로 마우스 인터랙션 추가

#### Day 4-7: useFrame Hook과 애니메이션 (5시간)

**목표**: 부드러운 애니메이션 구현

```typescript
import { useFrame } from '@react-three/fiber';
import { useRef } from 'react';
import * as THREE from 'three';

function AnimatedCube() {
  const meshRef = useRef<THREE.Mesh>(null);

  useFrame((state, delta) => {
    if (!meshRef.current) return;
    meshRef.current.rotation.x += delta * 0.5;
    meshRef.current.rotation.y += delta * 0.3;
  });

  return (
    <mesh ref={meshRef}>
      <boxGeometry args={[1, 1, 1]} />
      <meshStandardMaterial color="orange" />
    </mesh>
  );
}
```

**체크리스트**:
- [ ] useFrame의 delta 활용
- [ ] useRef로 mesh 참조
- [ ] 60fps 애니메이션 구현

---

### **Week 3: Blender 3D 모델링 기초** (10시간)

#### Day 1-3: Blender UI와 기본 조작 (5시간)

**목표**: Blender에서 간단한 로고 만들기

**무료 튜토리얼** (강력 추천):
- [Blender Guru - Donut Tutorial Part 1](https://www.youtube.com/watch?v=nIoXOplUvAw) - 3시간
- 세계에서 가장 유명한 Blender 입문 강좌 (1,000만+ 조회수)

**핵심 단축키**:
```
G: 이동 (Grab)
R: 회전 (Rotate)
S: 크기 조절 (Scale)
Tab: Edit Mode 전환
Shift+A: 오브젝트 추가
```

**실습 프로젝트**:
1. 기본 큐브 삭제 (X키)
2. Wing 로고 형태 모델링 (간단한 버전)
3. Material 적용 (파란색)
4. glTF로 익스포트

**체크리스트**:
- [ ] Blender UI 익숙해지기
- [ ] Edit Mode에서 Vertex 조작
- [ ] Material 색상 변경
- [ ] glTF Export 성공

#### Day 4-7: Wing 로고 3D 모델링 (5시간)

**목표**: 실제 사용할 Wing 로고 제작

**참고 이미지**:
- 기존 Wing 로고 PNG 파일 준비
- Blender에서 이미지 참조로 사용 (Add > Image > Reference)

**모델링 프로세스**:
1. **기본 형태 잡기** (1시간)
   - Bezier Curve로 로고 외곽선 따기
   - Convert to Mesh
   - Extrude (두께 주기)

2. **디테일 추가** (2시간)
   - Subdivision Surface Modifier
   - Smooth Shading
   - Edge 정리

3. **Material 설정** (1시간)
   - Wing 브랜드 컬러 적용 (#0066FF)
   - Metallic + Roughness 조정

4. **Export** (1시간)
   - File > Export > glTF 2.0
   - Format: glTF Binary (.glb)
   - Include: Selected Objects
   - Transform: +Y Up

**저장 위치**:
```
/Users/elicon_mark/Developer/01_2_elicon_homepage/public/models/wing-logo.glb
```

**체크리스트**:
- [ ] Wing 로고 형태 완성
- [ ] 브랜드 컬러 적용
- [ ] glb 파일 1MB 이하로 최적화
- [ ] Next.js public 폴더에 저장

---

### **Week 4: 엘리콘 홈페이지에 3D 통합** (10시간)

#### Day 1-3: Wing 로고 3D 컴포넌트 제작 (5시간)

**목표**: Hero 섹션에 회전하는 Wing 로고 추가

```typescript
// components/3d/WingLogo3D.tsx
'use client';
import { Canvas } from '@react-three/fiber';
import { useGLTF, OrbitControls, Environment } from '@react-three/drei';
import { Suspense } from 'react';

function WingModel() {
  const { scene } = useGLTF('/models/wing-logo.glb');

  return <primitive object={scene} scale={1.5} />;
}

export function WingLogo3D() {
  return (
    <div className="h-[500px] w-full">
      <Canvas camera={{ position: [0, 0, 5], fov: 50 }}>
        <Suspense fallback={null}>
          {/* 환경 조명 (현실감) */}
          <Environment preset="sunset" />

          {/* 3D 모델 */}
          <WingModel />

          {/* 마우스 컨트롤 */}
          <OrbitControls
            enableZoom={false}
            autoRotate
            autoRotateSpeed={2}
            maxPolarAngle={Math.PI / 2}
            minPolarAngle={Math.PI / 2}
          />
        </Suspense>
      </Canvas>
    </div>
  );
}
```

**Hero 섹션 통합**:
```typescript
// app/page.tsx
import { WingLogo3D } from '@/components/3d/WingLogo3D';

export default function HomePage() {
  return (
    <section className="hero">
      <div className="container grid grid-cols-2">
        {/* 왼쪽: 텍스트 */}
        <div>
          <h1>40년 경험의 전문 유통 파트너</h1>
          <p>Wing, Cocolic, Seanex 브랜드 총판</p>
        </div>

        {/* 오른쪽: 3D 로고 */}
        <WingLogo3D />
      </div>
    </section>
  );
}
```

**체크리스트**:
- [ ] WingLogo3D 컴포넌트 생성
- [ ] Hero 섹션에 통합
- [ ] 자동 회전 설정
- [ ] 로딩 상태 처리 (Suspense)

#### Day 4-7: 성능 최적화 (5시간)

**목표**: Lighthouse 95/100/95 유지

**1. Draco 압축 적용** (2시간)

```bash
# Draco 디코더 설치
npm install draco3dgltf

# public/draco/ 폴더에 디코더 복사
cp -r node_modules/draco3dgltf/draco/ public/draco/
```

```typescript
import { DRACOLoader } from 'three/examples/jsm/loaders/DRACOLoader';
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader';

const dracoLoader = new DRACOLoader();
dracoLoader.setDecoderPath('/draco/');

const gltfLoader = new GLTFLoader();
gltfLoader.setDRACOLoader(dracoLoader);
```

**Before/After**:
- 압축 전: wing-logo.glb (500KB)
- 압축 후: wing-logo-draco.glb (50KB) ✅ **90% 감소**

**2. Lazy Loading** (1시간)

```typescript
import dynamic from 'next/dynamic';

const WingLogo3D = dynamic(() => import('@/components/3d/WingLogo3D'), {
  ssr: false, // 서버 렌더링 비활성화
  loading: () => (
    <div className="h-[500px] flex items-center justify-center">
      <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600" />
    </div>
  ),
});
```

**3. 모바일 최적화** (2시간)

```typescript
'use client';
import { useMediaQuery } from '@/hooks/useMediaQuery';

export function WingLogo3D() {
  const isMobile = useMediaQuery('(max-width: 768px)');

  return (
    <Canvas
      dpr={isMobile ? 1 : 2} // 모바일은 픽셀 밀도 낮춤
      gl={{ antialias: !isMobile }} // 모바일은 안티앨리어싱 끔
    >
      <WingModel
        scale={isMobile ? 1 : 1.5}
        quality={isMobile ? 'low' : 'high'}
      />
    </Canvas>
  );
}
```

**체크리스트**:
- [ ] Draco 압축으로 파일 크기 90% 감소
- [ ] Lazy Loading으로 초기 로딩 속도 개선
- [ ] 모바일 최적화 (dpr=1, antialias=false)
- [ ] Lighthouse 성능 95점 이상 달성

---

### **Week 5: 인터랙션 고도화** (10시간)

#### Day 1-4: 마우스 호버 효과 (6시간)

**목표**: 마우스 올리면 로고 확대 + 강조

```typescript
'use client';
import { useRef, useState } from 'react';
import { useFrame } from '@react-three/fiber';
import * as THREE from 'three';

function InteractiveWing() {
  const meshRef = useRef<THREE.Group>(null);
  const [hovered, setHovered] = useState(false);

  useFrame(() => {
    if (!meshRef.current) return;

    // 호버 시 부드럽게 확대
    const targetScale = hovered ? 1.2 : 1;
    meshRef.current.scale.lerp(
      new THREE.Vector3(targetScale, targetScale, targetScale),
      0.1
    );
  });

  return (
    <group
      ref={meshRef}
      onPointerEnter={() => setHovered(true)}
      onPointerLeave={() => setHovered(false)}
    >
      <WingModel />
    </group>
  );
}
```

**추가 효과**:
- 호버 시 회전 속도 증가
- 클릭 시 제품 페이지로 이동

**체크리스트**:
- [ ] onPointerEnter/Leave 이벤트 처리
- [ ] lerp로 부드러운 스케일 변화
- [ ] 커서 변경 (cursor: pointer)

#### Day 5-7: 스크롤 인터랙션 (4시간)

**목표**: 스크롤에 따라 로고 회전

```typescript
'use client';
import { useScroll } from '@react-three/drei';
import { useFrame } from '@react-three/fiber';

function ScrollControlledWing() {
  const meshRef = useRef<THREE.Group>(null);
  const scroll = useScroll();

  useFrame(() => {
    if (!meshRef.current) return;

    // 스크롤 진행률 (0~1)
    const offset = scroll.offset;

    // Y축 회전 (360도)
    meshRef.current.rotation.y = offset * Math.PI * 2;
  });

  return <primitive ref={meshRef} object={scene} />;
}
```

**체크리스트**:
- [ ] useScroll Hook 활용
- [ ] 스크롤 진행률에 따른 회전
- [ ] 부드러운 전환 (easing)

---

### **Week 6: 최종 최적화 및 제조사 데모 준비** (10시간)

#### Day 1-3: 품질 검증 (4시간)

**Lighthouse 테스트**:
```bash
npm run build
npm start

# Chrome DevTools > Lighthouse
# Performance, Accessibility, Best Practices 모두 95+ 목표
```

**체크리스트**:
- [ ] Performance: 95+
- [ ] Accessibility: 100
- [ ] Best Practices: 95+
- [ ] 모바일/데스크톱 모두 테스트

#### Day 4-5: 제조사 데모 시나리오 작성 (3시간)

**한국OGK용 데모 스크립트**:

```markdown
# Wing 브랜드 3D 프레젠테이션 (2분)

1. **인트로** (15초)
   "안녕하세요, 엘리콘입니다. 오늘 Wing 브랜드의
   새로운 온라인 쇼케이스를 보여드리겠습니다."

2. **Hero 섹션** (30초)
   - Wing 로고 3D 자동 회전 시연
   - "마우스로 직접 로고를 회전시킬 수 있습니다"
   - 호버 효과 시연 (확대)

3. **제품 뷰어** (45초)
   - 헬멧 360도 회전
   - 색상 옵션 변경
   - "고객이 매장 방문 없이 제품을 자세히 볼 수 있습니다"

4. **기술 설명** (30초)
   - "최신 WebGL 기술 사용"
   - "모바일에서도 빠르게 로딩"
   - "Lighthouse 성능 95점 이상"

5. **마무리**
   "엘리콘은 단순 유통사가 아니라,
   브랜드 가치를 높이는 기술 파트너입니다."
```

**써모랩용 데모** (Cocolic/Seanex):
- 유사한 스크립트, 제품만 변경
- 파트너 네트워크 3D 지구본 추가 (선택)

#### Day 6-7: 백업 및 문서화 (3시간)

**문서 작성**:
```markdown
# Wing 로고 3D 구현 가이드

## 파일 구조
/components/3d/
  - WingLogo3D.tsx
  - InteractiveWing.tsx
  - ScrollControlledWing.tsx

/public/models/
  - wing-logo.glb (500KB)
  - wing-logo-draco.glb (50KB)

## 사용법
import { WingLogo3D } from '@/components/3d/WingLogo3D';

<WingLogo3D />

## 성능 지표
- Lighthouse Performance: 96
- First Contentful Paint: 1.2s
- Largest Contentful Paint: 2.1s
```

**체크리스트**:
- [ ] 코드 주석 추가
- [ ] README.md 업데이트
- [ ] 데모 시나리오 문서화
- [ ] 백업 (Git commit)

---

## 💰 무료 vs 유료 리소스 가이드

### 무료 리소스 (총 $0)

| 항목 | 리소스 | 시간 투자 |
|-----|--------|----------|
| **Three.js 기초** | [Three.js 공식 문서](https://threejs.org/manual/) | 10시간 |
| **Blender 학습** | [Blender Guru - Donut](https://www.youtube.com/watch?v=nIoXOplUvAw) | 5시간 |
| **React Three Fiber** | [공식 문서](https://docs.pmnd.rs/react-three-fiber) | 8시간 |
| **3D 모델** | [Sketchfab Free](https://sketchfab.com/features/free-3d-models) | 1시간 |
| **총 투자** | $0 | **24시간** |

**장점**: 비용 없음
**단점**: 학습 곡선 높음, 체계적 커리큘럼 부족

### 유료 리소스 (총 $104)

| 항목 | 리소스 | 가격 | 시간 투자 |
|-----|--------|------|----------|
| **Three.js Journey** | [threejs-journey.com](https://threejs-journey.com) | $95 (평생) | 15시간 |
| **Spline** | [spline.design](https://spline.design/pricing) | $9/월 (6개월) | 3시간 |
| **총 투자** | **$149** | **18시간** |

**장점**: 학습 속도 3배, 체계적 커리큘럼, 최신 트렌드
**단점**: 초기 비용

### 추천 전략 (하이브리드)

**Phase 1: 무료 리소스로 시작** (Week 1-2)
- Three.js 공식 문서로 기초 다지기
- Blender Guru로 모델링 입문
- 투자: $0, 시간: 15시간

**Phase 2: 유료 리소스로 가속** (Week 3-6)
- Three.js Journey 구매 ($95) → 학습 시간 50% 단축
- Spline 1개월 구독 ($9) → 빠른 프로토타입
- 투자: $104, 시간: 18시간 절약

**ROI 분석**:
```
무료: $0 + 40시간 = $0 (시간 기회비용 높음)
유료: $104 + 22시간 = $104 (시간 기회비용 낮음)

시간당 가치 $50 기준:
무료: 40시간 × $50 = $2,000 기회비용
유료: 22시간 × $50 = $1,100 기회비용

실제 절감: $900 - $104 = $796 절약 ✅
```

---

## ✅ 실습 프로젝트 체크리스트

### Level 1: 기초 (Week 1-2)

- [ ] **프로젝트 1**: 회전하는 큐브 (Three.js 순수)
  - [ ] Scene, Camera, Renderer 설정
  - [ ] BoxGeometry + Material
  - [ ] Animation Loop

- [ ] **프로젝트 2**: 조명 실험
  - [ ] Ambient + Directional + Point Light
  - [ ] 그림자 활성화
  - [ ] Color와 Intensity 조절

- [ ] **프로젝트 3**: glTF 모델 로드
  - [ ] GLTFLoader 사용
  - [ ] 무료 모델 다운로드
  - [ ] Scene에 추가

### Level 2: 통합 (Week 3-4)

- [ ] **프로젝트 4**: React Three Fiber 전환
  - [ ] Canvas 컴포넌트
  - [ ] useFrame Hook
  - [ ] OrbitControls

- [ ] **프로젝트 5**: Wing 로고 3D 모델링
  - [ ] Blender에서 로고 제작
  - [ ] Material 설정
  - [ ] glTF Export

- [ ] **프로젝트 6**: Next.js 통합
  - [ ] WingLogo3D 컴포넌트
  - [ ] Hero 섹션에 삽입
  - [ ] Lazy Loading

### Level 3: 고급 (Week 5-6)

- [ ] **프로젝트 7**: 인터랙션
  - [ ] 마우스 호버 효과
  - [ ] 클릭 이벤트
  - [ ] 스크롤 인터랙션

- [ ] **프로젝트 8**: 성능 최적화
  - [ ] Draco 압축
  - [ ] 모바일 최적화
  - [ ] Lighthouse 95+

- [ ] **프로젝트 9**: 제조사 데모
  - [ ] 데모 시나리오 작성
  - [ ] 프레젠테이션 준비
  - [ ] 피드백 수집

---

## 🔧 트러블슈팅 가이드

### 문제 1: "Module not found: Can't resolve 'three'"

**원인**: Three.js 미설치

**해결**:
```bash
npm install three @types/three
```

### 문제 2: "useGLTF is not a function"

**원인**: @react-three/drei 미설치

**해결**:
```bash
npm install @react-three/drei
```

### 문제 3: glTF 모델이 보이지 않음

**체크리스트**:
- [ ] 파일 경로 확인 (`/public/models/` 내부?)
- [ ] 카메라 위치 조정 (`position={[0, 0, 5]}`)
- [ ] 조명 추가 (`<ambientLight />`)
- [ ] 스케일 조정 (`scale={0.5}`)

**디버깅**:
```typescript
loader.load(
  '/models/wing-logo.glb',
  (gltf) => {
    console.log('Model loaded:', gltf); // 성공
    console.log('Bounding box:', gltf.scene.children[0].geometry.boundingBox); // 크기 확인
  },
  undefined,
  (error) => {
    console.error('Error:', error); // 실패
  }
);
```

### 문제 4: 성능 저하 (FPS < 30)

**체크리스트**:
- [ ] 폴리곤 수 줄이기 (Blender에서 Decimate Modifier)
- [ ] Texture 크기 줄이기 (2048px → 512px)
- [ ] Draco 압축 적용
- [ ] 모바일에서 dpr=1 설정

### 문제 5: Blender Export 오류

**확인사항**:
- [ ] File > Export > glTF 2.0 선택
- [ ] Format: glTF Binary (.glb)
- [ ] Include: Selected Objects만
- [ ] Transform: +Y Up 체크

---

## 📊 학습 진도 추적표

| Week | 목표 | 시간 | 완료 | 비고 |
|------|------|------|------|------|
| 1 | Three.js 기초 | 10h | ☐ | Scene, Camera, Renderer |
| 2 | React Three Fiber | 10h | ☐ | Canvas, useFrame |
| 3 | Blender 모델링 | 10h | ☐ | Wing 로고 제작 |
| 4 | Next.js 통합 | 10h | ☐ | Hero 섹션 3D |
| 5 | 인터랙션 | 10h | ☐ | 호버, 스크롤 |
| 6 | 최적화 + 데모 | 10h | ☐ | Lighthouse 95+ |

**진행률**: 0 / 60 시간 (0%)

---

## 🎯 최종 목표 재확인

**6주 후 달성할 성과**:

1. ✅ **Wing 로고 3D 웹사이트 구현**
   - 자동 회전 + 마우스 인터랙션
   - Lighthouse 95/100/95 성능
   - 모바일 최적화

2. ✅ **제조사 데모 준비 완료**
   - 한국OGK용 프레젠테이션
   - 써모랩용 프레젠테이션
   - 2분 데모 스크립트

3. ✅ **기술력 입증**
   - "엘리콘은 3D까지 투자하는 고급 파트너"
   - 경쟁사 대비 차별화
   - 제조사 신뢰 확보

**예상 비즈니스 임팩트**:
- 제조사 미팅 시 첫인상 개선
- 브랜드 가치 향상 (Wing, Cocolic, Seanex)
- 추가 제품 라인업 제안 기회 증가
- **연간 매출 증대 예상**: 1.5억원

---

## 📚 참고 자료

### 공식 문서
- [Three.js 공식 문서](https://threejs.org/docs/)
- [React Three Fiber 공식 문서](https://docs.pmnd.rs/react-three-fiber)
- [Blender 공식 문서](https://docs.blender.org/)

### 커뮤니티
- [Three.js Discourse](https://discourse.threejs.org/)
- [React Three Fiber Discord](https://discord.gg/poimandres)
- [Blender Artists](https://blenderartists.org/)

### YouTube 채널
- **Bruno Simon** (Three.js Journey 제작자)
- **Blender Guru** (Blender 튜토리얼)
- **Wael Yasmina** (Three.js 실전)

---

**최종 업데이트**: 2025-12-03
**다음 단계**: Week 1 Day 1 시작 - Three.js Fundamentals 읽기
**문의**: logs/ 폴더에 학습 진행 상황 기록 권장
