# ELICON Project Upgrade Kit - Makefile
# 명령 통일: check가 곧 게이트

.PHONY: check run test deploy clean help

# 기본 명령
help:
	@echo "ELICON Project Upgrade Kit"
	@echo "=========================="
	@echo ""
	@echo "사용 가능한 명령:"
	@echo "  make check   - Gate 체크 실행 (필수)"
	@echo "  make run     - 프로젝트 실행"
	@echo "  make test    - 테스트 실행"
	@echo "  make deploy  - 배포"
	@echo "  make clean   - 정리"

# Gate 체크 (필수)
check:
	@echo "🔍 Gate Check 실행..."
	@python3 scripts/gate_check.py

# 프로젝트 실행 (프로젝트별 정의)
run:
	@echo "ℹ️  프로젝트별 run 명령을 정의하세요"
	@echo "   예: python3 main.py"

# 테스트 (프로젝트별 정의)
test:
	@echo "ℹ️  프로젝트별 test 명령을 정의하세요"
	@echo "   예: pytest tests/"

# 배포 (프로젝트별 정의)
deploy:
	@echo "ℹ️  프로젝트별 deploy 명령을 정의하세요"

# 정리
clean:
	@find . -type f -name "*.pyc" -delete
	@find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
	@echo "✅ 정리 완료"
